# numberWordConvert

Install:
```sh
npm install
bower install
npm -install gulp -g
```

Build Command:
```sh
gulp build-dev
gulp build-prod
```

Unit Test (XUnit require targeting dnx 1.0.0-rc1-update1):

```sh
dnx test
```
